import {StyleSheet, TouchableOpacity, Pressable} from 'react-native';
import colors from '@/constant/colors';


export const sign_styles = StyleSheet.create({
    textInput: {
        borderWidth: 1,
        borderColor: colors.PRIMARY_O,
        width: '100%',
        fontFamily: 'PixeloidSans',
        padding: 10,
        fontSize: 18,
        marginTop: 20,
        borderRadius: 10,
    }
});